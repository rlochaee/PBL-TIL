# TIL 1주차

## 1. 배운 내용
- 객체지향 프로그래밍이란 무엇인지 배우고, 주요 문법에 대한 개념 정리, 자바를 이용한 프로그래밍 기초를 배우고, 프로그램 코드 구현하는 것을 진행했다.

## 2. 핵심 정리
- 자바의 모든 코드는 클래스 안에 있어야 함. 파일 이름이 Main이라면 클래스 이름도 Main 이어야 함
main 함수 선언 -> 프로그램의 시작버튼/컴퓨터는 이 문장 안의 코드부터 실행
- System.out.println() -> 원하는 내용 출력
- 변수: 데이터 저장 공간 -> 자료형에 따라 크기 달라짐
    
    기본형 변수
    byte(1 byte 정수형 데이터 타입)
    short(2 byte 정수형 데이터 타입)
    int(4 byte 정수형 데이터 타입)
    long(8 byte 정수형 데이터 타입)
    -> 위 범위값을 벗어난 값을 대입할 경우 에러 발생

    double(8 byte 실수형 데이터 타입)- 15자리
    float(4 byte 실수형 데이터 타입)-7자리, 소수점 뒤에 f 붙여줘야 함

    char(2 byte 문자형 데이터 타입)-''로 문자 써주기, 아스키코드, 유니코드 넣어도 됨

    boolean(1 byte 논리형 데이터 타입)-참/거짓값, 1이나 0 대입x

    참조형 변수
    String -> new 연산자를 이용해 객체 생성
    참조형 데이터 값은 힙 메모리에 저장되어 변수에는 메모리 주소가 대입됨

- 상수: 한 번 값이 할당되면 재할당될 수x/전체를 대문자, 단어간 구문은 _
    constants/final variables
- 형변환 
    byte<short<int<long<<<float<double순서

- 조건문
    if문
    if (조건식) {
    조건이 참(true)일 때 실행
} else if (다른_조건식) {
앞의 조건은 거짓이고, 이 조건이 참일 때 실행
} else {
모든 조건이 거짓일 때 실행
}
switch문
switch (변수) {
case 값1:
변수가 값1일 때 실행
break;
case 값2:
변수가 값2일 때 실행
break;
default:
어떤 케이스에도 해당하지 않을 때 실행
}
- 반복문
for - 반복 횟수가 명확할 때 
for (초기화; 조건식; 증감식) { 실행할 코드 } 구조

while - 조건이 참인 동안 반복
while (조건식) { 실행할 코드 } 구조
break 반복문 즉시 종료
continue 현재 실행 중인 코드 멈추고 반복문 처음으로 돌아감
## 3. PBL 결과 이미지
![alt text](<스크린샷 2026-04-05 000852-3.png>)
![alt text](<스크린샷 2026-04-05 001107-2.png>)
![alt text](<스크린샷 2026-04-05 001115-1.png>) 
## 4. 느낀 점!
- 자바에 대해 아예 모르는 상태였어서 세션을 잘 따라갈 수 있을지 걱정됐었는데, 다른 언어들이랑 비슷한(?) 느낌도 있었어서 생각보단 어렵진 않았던 것 같았다.. 아마도? 그래도 PBL도 해보고 강의도 따로 들으면서 자바의 기본 문법도 익히고, 이를 실제 문제에 적용해 보면서 조금은 알 것 같기도 하다. 그래도 객체지향 프로그램을 처음 다뤄봐서 신기하기도 하고 낯설기도 하고 여러 생각이 많아졌다 그래도 계속 하다보면 익숙해지겠지~~ 하는 마음으로 일단 부딪혀보기로 함!! 